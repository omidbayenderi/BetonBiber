# Admin panel password — set this to the SHA-256 hash of your desired password.
# Generate: echo -n "yourpassword" | shasum -a 256
# Copy this file to .env.local and set your own hash.
VITE_ADMIN_PASS_HASH=your_sha256_hash_here
